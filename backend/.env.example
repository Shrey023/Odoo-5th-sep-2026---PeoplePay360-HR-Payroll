# Postgres (matches docker-compose.yml)
DATABASE_URL="postgresql://peoplepay:peoplepay@localhost:5432/peoplepay360?schema=public"

# Server
PORT=4000

# Auth
JWT_SECRET="change-me-in-real-life"
JWT_EXPIRES_IN="1d"

# Email (Mailtrap / Ethereal for demo - fill from your sandbox inbox)
SMTP_HOST="sandbox.smtp.mailtrap.io"
SMTP_PORT=2525
SMTP_USER=""
SMTP_PASS=""
MAIL_FROM="PeoplePay360 <payroll@peoplepay360.test>"
